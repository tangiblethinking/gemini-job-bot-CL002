# Ape X Job Hunt — CL002

Build 1: Core flow parity with CL001.

- API keys → resume upload → title extraction → title chips → job search → results
- ATS Resume generates in new tab via blob URL
- Branding updated to Ape X Job Hunt with new logo and favicon
- Light theme (apple-inspired neutral palette)

## Stack
Next.js 15, React 19, TypeScript, Tailwind CSS v3, Gemini 2.5 Flash, Serper
